From 3c50f4b073a37fa126db9e4cef289fb5c5719153 Mon Sep 17 00:00:00 2001
From: Euphyllia Bierque <bierque.euphyllia@gmail.com>
Date: Tue, 26 May 2026 15:49:13 +0200
Subject: [PATCH 15/22] Patch Backup

---
 .../java/com/earth2me/essentials/Backup.java  | 24 +++++++++----------
 1 file changed, 12 insertions(+), 12 deletions(-)

diff --git a/Essentials/src/main/java/com/earth2me/essentials/Backup.java b/Essentials/src/main/java/com/earth2me/essentials/Backup.java
index a6831ab3b..e33596036 100644
--- a/Essentials/src/main/java/com/earth2me/essentials/Backup.java
+++ b/Essentials/src/main/java/com/earth2me/essentials/Backup.java
@@ -18,7 +18,7 @@ public class Backup implements Runnable {
     private transient final IEssentials ess;
     private final AtomicBoolean pendingShutdown = new AtomicBoolean(false);
     private transient boolean running = false;
-    private transient int taskId = -1;
+    private transient io.papermc.paper.threadedregions.scheduler.ScheduledTask taskId;
     private transient boolean active = false;
     private transient CompletableFuture<Object> taskLock = null;
 
@@ -26,7 +26,7 @@ public class Backup implements Runnable {
         this.ess = ess;
         server = ess.getServer();
         if (!ess.getOnlinePlayers().isEmpty() || ess.getSettings().isAlwaysRunBackup()) {
-            ess.runTaskAsynchronously(this::startTask);
+            ess.getAsyncScheduler().runNow(ess, scheduledTask -> startTask());
         }
     }
 
@@ -36,10 +36,10 @@ public class Backup implements Runnable {
 
     public synchronized void stopTask() {
         running = false;
-        if (taskId != -1) {
-            server.getScheduler().cancelTask(taskId);
+        if (taskId != null) {
+            taskId.cancel();
         }
-        taskId = -1;
+        taskId = null;
     }
 
     private synchronized void startTask() {
@@ -48,7 +48,7 @@ public class Backup implements Runnable {
             if (interval < 1200) {
                 return;
             }
-            taskId = ess.scheduleSyncRepeatingTask(this, interval, interval);
+            taskId = ess.getGlobalRegionScheduler().runAtFixedRate(ess, scheduledTask -> this.run(), interval, interval);
             running = true;
         }
     }
@@ -74,23 +74,23 @@ public class Backup implements Runnable {
         taskLock = new CompletableFuture<>();
         if ("save-all".equalsIgnoreCase(command)) {
             final CommandSender cs = server.getConsoleSender();
-            server.dispatchCommand(cs, "save-all");
+            ess.getGlobalRegionScheduler().execute(ess, () -> server.dispatchCommand(cs, "save-all"));
             active = false;
             taskLock.complete(new Object());
             return;
         }
         ess.getLogger().log(Level.INFO, ess.getAdventureFacet().miniToLegacy(tlLiteral("backupStarted")));
-        final CommandSender cs = server.getConsoleSender();
+        final CommandSender cs = server.getConsoleSender();ess.getGlobalRegionScheduler().execute(ess, () -> {
         server.dispatchCommand(cs, "save-all");
-        server.dispatchCommand(cs, "save-off");
+        server.dispatchCommand(cs, "save-off");});
 
-        ess.runTaskAsynchronously(() -> {
+        ess.getAsyncScheduler().runNow(ess, scheduledTask -> {
             try {
                 final ProcessBuilder childBuilder = new ProcessBuilder(command.split(" "));
                 childBuilder.redirectErrorStream(true);
                 childBuilder.directory(ess.getDataFolder().getParentFile().getParentFile());
                 final Process child = childBuilder.start();
-                ess.runTaskAsynchronously(() -> {
+                ess.getAsyncScheduler().runNow(ess, scheduledTask2 -> {
                     try {
                         try (final BufferedReader reader = new BufferedReader(new InputStreamReader(child.getInputStream()))) {
                             String line;
@@ -123,7 +123,7 @@ public class Backup implements Runnable {
                 }
 
                 if (!pendingShutdown.get()) {
-                    ess.scheduleSyncDelayedTask(new BackupEnableSaveTask());
+                    ess.getGlobalRegionScheduler().execute(ess, new BackupEnableSaveTask());
                 }
             }
         });
-- 
2.54.0.windows.1

