From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Euphyllia Bierque <bierque.euphyllia@gmail.com>
Date: Sat, 6 Jun 2026 09:24:38 +0200
Subject: [PATCH] Fix eject entity


diff --git a/Essentials/src/main/java/com/earth2me/essentials/AsyncTeleport.java b/Essentials/src/main/java/com/earth2me/essentials/AsyncTeleport.java
index e67476075cb4e86f39c3a440585cde3056cc1f1c..7f6be1c29a349f5b63d9f75a77ce670383ed5d0e 100644
--- a/Essentials/src/main/java/com/earth2me/essentials/AsyncTeleport.java
+++ b/Essentials/src/main/java/com/earth2me/essentials/AsyncTeleport.java
@@ -5,6 +5,7 @@ import com.earth2me.essentials.commands.WarpNotFoundException;
 import com.earth2me.essentials.utils.DateUtil;
 import com.earth2me.essentials.utils.LocationUtil;
 //import io.papermc.lib.PaperLib;
+import com.earth2me.essentials.utils.VersionUtil;
 import net.ess3.api.IEssentials;
 import net.ess3.api.IUser;
 import net.ess3.api.TranslatableException;
@@ -140,6 +141,7 @@ public class AsyncTeleport implements IAsyncTeleport {
     }
 
     private void runOnMain(final Runnable runnable) throws ExecutionException, InterruptedException {
+        if (VersionUtil.isFoliaServer()) throw new UnsupportedOperationException("Not supported on Folia");
         if (Bukkit.isPrimaryThread()) {
             runnable.run();
             return;
@@ -169,8 +171,8 @@ public class AsyncTeleport implements IAsyncTeleport {
             }
 
             try {
-                runOnMain(() -> teleportee.getBase().eject()); //EntityDismountEvent requires a sync context.
-            } catch (final ExecutionException | InterruptedException e) {
+                teleportee.getBase().getScheduler().run(ess, (scheduledTask) -> teleportee.getBase().eject(), null); //EntityDismountEvent requires a sync context.
+            } catch (final Exception e) {
                 future.completeExceptionally(e);
                 return;
             }
